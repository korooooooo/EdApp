# Default: use mock AI and do not call /api.
VITE_USE_MOCK_AI=true

# Set VITE_USE_MOCK_AI=false to use NVIDIA NIM through the server API.
# Do not add VITE_ to NVIDIA_API_KEY.
NVIDIA_API_KEY=replace-with-your-nvidia-api-key
NVIDIA_MODEL=deepseek-ai/deepseek-v4-pro
